# windows/adcs

ADCS enumeration and abuse tools to drop on Windows targets.

- `Certify.exe` — GhostPack ADCS enumeration (no official compiled release — compile from source or place manually)

Pairs with `certipy` on the Linux/container side.
