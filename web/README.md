# web

Put shared web resources here.

Recommended subfolders:

- `webshells/`
- `payloads/`
- `templates/`
- `graphql/`
- `fuzzing/`

If a resource is generic and reused often, it belongs here.
If it is engagement-specific, keep it in the workspace instead.
