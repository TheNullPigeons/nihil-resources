# bin

Put tiny helper wrappers here.

This directory is added to `PATH` by `setup/zsh/zshrc`, so anything here becomes directly callable in Nihil shells.

Good candidates:

- wrappers around common resource paths
- small fetch helpers
- convenience scripts that glue existing tools together
